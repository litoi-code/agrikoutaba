
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for MVP: anyone can read or write any document.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
