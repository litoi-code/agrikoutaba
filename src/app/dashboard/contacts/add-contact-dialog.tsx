
"use client";

// This component is now located at src/app/[locale]/dashboard/contacts/add-contact-dialog.tsx
export function AddContactDialog({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
