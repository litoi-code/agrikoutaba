
"use client";

// This component is now located at src/app/[locale]/dashboard/tasks/add-task-dialog.tsx
export function AddTaskDialog({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
