
"use client";

// This component is now located at src/app/[locale]/dashboard/finances/add-transaction-dialog.tsx
export function AddTransactionDialog({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
