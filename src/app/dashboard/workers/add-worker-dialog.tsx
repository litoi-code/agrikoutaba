
"use client";

// This component is now located at src/app/[locale]/dashboard/workers/add-worker-dialog.tsx
export function AddWorkerDialog({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
